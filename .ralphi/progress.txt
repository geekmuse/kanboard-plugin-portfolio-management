## Codebase Patterns
- Event listener tests: use FakeDispatcher.listeners[eventName][0](array) to invoke registered callbacks with an ArrayAccess-compatible plain array (no Kanboard runtime needed).
- DependencyModel event chain: `onTaskClosed` calls `dispatchDependencyResolvedEvent` which routes through the container's 'dispatcher' service (if it has a `dispatch()` method) — use DependencyTestEventDispatcher or FakeDispatcher in tests.
- Deep behavioral tests for Plugin event wiring live in `Test/Plugin/ApiRegistrationTest.php`; per-method DependencyModel behaviour tests live in `Test/Model/DependencyModelTest.php`.
- When an acceptance criterion is already satisfied by an existing test, verify the mapping explicitly, run all quality gates, and mark the story done without adding redundant tests.
---

## PRD Run Context
PRD Branch: ralph/code-health-integration
Started: 2026-03-25T21:26:36.616Z
---

## 2026-03-25 — US-001
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Verified all 8 acceptance criteria are already satisfied by existing tests:
  - Criteria 1–4 (listener wiring + task_id delegation): `testRegistersDependencyLifecycleListenersAndDelegatesToDependencyModel` in `Test/Plugin/ApiRegistrationTest.php`
  - Criterion 5 (portfolio.dependency.resolved dispatch): `testOnTaskClosedFiresDependencyResolvedEvent` in `Test/Model/DependencyModelTest.php`
  - Criteria 6–8 (stubs only, 172 tests pass, PHPCS/PHPStan/lint clean): confirmed by running all quality gates
- No source code changes were required; the tests were implemented as part of the v1.21.0 planning work.
- Files verified (no edits needed): `Test/Plugin/ApiRegistrationTest.php`, `Test/Model/DependencyModelTest.php`
- **Learnings for future iterations:**
  - The PRD notes section may describe prior state ("already covers X"); always check acceptance criteria directly against existing code/tests before writing new code.
  - `testRegistersDependencyLifecycleListenersAndDelegatesToDependencyModel` provides full behavioral validation (not just registration): it invokes the listener closures with fake event data and asserts delegation to the model.
  - DependencyModel's `dispatchDependencyResolvedEvent()` checks for `dispatch()` first (Symfony-style dispatcher), then falls back to `fire()` (Kanboard eventManager) — tests must supply a dispatcher with `dispatch()` to capture events reliably.
---

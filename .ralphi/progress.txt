## Codebase Patterns
- `DependencyTestQueryBuilder` in DependencyModelTest now has `in()` method (same shape as `PortfolioTaskTestQueryBuilder`): stores `operator='IN'` + value array; `buildWhereClause()` dispatches on `IN` vs scalar operator. Follow this pattern for any new IN-clause needs in DependencyModel tests.
- `getDependencies()` IN-scoping pattern: fetch portfolio task IDs from `tasks->in('project_id', $projectIds)`, guard empty result, then use `task_has_links->in('task_id', $portfolioTaskIds)` with >1000 fallback to full-table scan. Same two-step pattern as `buildFilteredTaskRows()` + `buildTaskDependencyStats()` in PortfolioTaskModel.

- `task.update` and `task.assignee_change` stub listeners follow the same pattern as other lifecycle listeners: read task_id, guard `<= 0`, delegate to model method. The model method itself is a no-op stub documented with TODO for future implementation.
- `FakePortfolioTaskModel` in ApiRegistrationTest: exposes `$assigneeChangedCalls[]` public array; set in container after `initialize()` for listener tests. Same pattern as `FakeDependencyModel`.
- `FakeDependencyModel::$updatedCalls[]` records `onTaskUpdated` call arguments (int task_id) — same pattern as existing `closedCalls`, `openedCalls`, `linkChangedCalls` arrays.
- Pre-commit phpstan timeout pattern confirmed again: warm the Docker image with one manual `bash scripts/docker-phpstan.sh` run before committing when image is cold.
- `task.move.column` event listener pattern: read `column_title` from event (lowercase/trim), check against `$donePatterns` with `str_contains`, then check all milestone tasks for `is_active=0`. Fall back to DB lookup via `column_id` if column_title is empty. Guard order: task_id → config → done-column → milestone lookup.
- `FakeMilestoneTaskModel` in ApiRegistrationTest now supports `getMilestones()` and `getTasks()` via `$milestonesForTask` / `$tasksForMilestone` public arrays. Set these before invoking the listener under test.
- `FakeMilestoneModel` in ApiRegistrationTest records `update()` calls in `$updateCalls` as `[$milestoneId, $values]` tuples — same pattern as `FakePortfolioModel::$updateCalls`.
- `FakeConfigModel` in ApiRegistrationTest: construct with `['key' => value]` map; `get($key, $default)` returns configured value or default. Set `$this->plugin->container['configModel']` after `initialize()` to override config for specific tests.
- Pre-commit hook (ralphi check) can timeout on phpstan if Docker image is cold; run `ralphi check` manually first to warm the image, then commit.


- Task form hooks use `template:task:form:first-column` to inject fields into Kanboard's task creation form; `params['project_id']` carries the project context (also check `params['task']['project_id']` defensively).
- `task.create` event receives all form values including custom fields added by hook templates; listener can read `$event['milestone_id']` to process post-creation side effects.
- Widget templates that receive pre-fetched data via `attachCallable` follow the pattern: check `! empty($varName)` at top → render content → close endif; this keeps templates minimal and testable.
- New FakeMilestoneTaskModel in ApiRegistrationTest: `$addCalls[]` records `[$milestoneId, $taskId]` tuples; set in container after `initialize()` for listener behavioral tests.
- Event listener tests: use FakeDispatcher.listeners[eventName][0](array) to invoke registered callbacks with an ArrayAccess-compatible plain array (no Kanboard runtime needed).
- DependencyModel event chain: `onTaskClosed` calls `dispatchDependencyResolvedEvent` which routes through the container's 'dispatcher' service (if it has a `dispatch()` method) — use DependencyTestEventDispatcher or FakeDispatcher in tests.
- Deep behavioral tests for Plugin event wiring live in `Test/Plugin/ApiRegistrationTest.php`; per-method DependencyModel behaviour tests live in `Test/Model/DependencyModelTest.php`.
- When an acceptance criterion is already satisfied by an existing test, verify the mapping explicitly, run all quality gates, and mark the story done without adding redundant tests.
- `template:task:show:top` and `template:project:header:after` hooks receive `params['task']`/`params['project']` respectively — use the same defensive `$params['task'] ?? $params` and `$params['project'] ?? $params` patterns as other task/project hooks.
- `template:board:task:icons` callable is identical to `board:task:footer` callable — same config check, same `portfolioHelper->isTaskBlocked()` call, same `$params['task'] ?? $params` param extraction. PortfolioHelper lazy-loads per project on first call, so the second hook (icons) hits cache when footer already ran.
- When adding new template hooks to ApiRegistrationTest: update BOTH `testRegistersAllRequiredTemplateHooks` (assertContains checks) AND `testTemplateHooksPointToCorrectWidgetTemplates` (assertSame on templateByHook map).
---

## PRD Run Context
PRD Branch: ralph/code-health-integration
Started: 2026-03-25T21:26:36.616Z
---

## 2026-03-25 — US-001
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Verified all 8 acceptance criteria are already satisfied by existing tests:
  - Criteria 1–4 (listener wiring + task_id delegation): `testRegistersDependencyLifecycleListenersAndDelegatesToDependencyModel` in `Test/Plugin/ApiRegistrationTest.php`
  - Criterion 5 (portfolio.dependency.resolved dispatch): `testOnTaskClosedFiresDependencyResolvedEvent` in `Test/Model/DependencyModelTest.php`
  - Criteria 6–8 (stubs only, 172 tests pass, PHPCS/PHPStan/lint clean): confirmed by running all quality gates
- No source code changes were required; the tests were implemented as part of the v1.21.0 planning work.
- Files verified (no edits needed): `Test/Plugin/ApiRegistrationTest.php`, `Test/Model/DependencyModelTest.php`
- **Learnings for future iterations:**
  - The PRD notes section may describe prior state ("already covers X"); always check acceptance criteria directly against existing code/tests before writing new code.
  - `testRegistersDependencyLifecycleListenersAndDelegatesToDependencyModel` provides full behavioral validation (not just registration): it invokes the listener closures with fake event data and asserts delegation to the model.
  - DependencyModel's `dispatchDependencyResolvedEvent()` checks for `dispatch()` first (Symfony-style dispatcher), then falls back to `fire()` (Kanboard eventManager) — tests must supply a dispatcher with `dispatch()` to capture events reliably.
---

## 2026-03-25 — US-002
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Registered `template:task:form:first-column` hook via `attachCallable` in Plugin.php
- Callable pre-fetches portfolios (one `getPortfolios()` call) then milestones (one `getByPortfolioId()` per portfolio) — no N+1
- Returns `['milestones' => []]` when project belongs to no portfolios; template guards with `! empty($milestones)`
- Registered `task.create` dispatcher listener; reads `milestone_id` from event, calls `milestoneTaskModel->add()` if both IDs > 0
- Created `Template/widget/task_form_milestone_dropdown.php` — standard `<select>` with `portfolio-milestone-form-field` CSS class
- Added `'None' => 'None'` to `Locale/en_US/translations.php`
- Added 4 new tests (176 total): 2 template rendering (`WidgetHookTest`) + 2 event listener behavioral (`ApiRegistrationTest`)
- Updated `testRegistersAllRequiredTemplateHooks` and `testTemplateHooksPointToCorrectWidgetTemplates` in ApiRegistrationTest
- Added `FakeMilestoneTaskModel` to ApiRegistrationTest for listener tests
- Files changed: `Plugin.php`, `Template/widget/task_form_milestone_dropdown.php`, `Locale/en_US/translations.php`, `Test/Controller/WidgetHookTest.php`, `Test/Plugin/ApiRegistrationTest.php`
- **Learnings for future iterations:**
  - Task creation form hook provides `params['project_id']` directly (also defensively check `params['task']['project_id']`)
  - `task.create` event data includes all form values; custom fields like `milestone_id` flow through from POST to event
  - `FakeMilestoneTaskModel` pattern: record `[$milestoneId, $taskId]` tuples in `$addCalls` for assertion; set in container AFTER `initialize()` runs (overrides the registered closure)
  - Closures inside `initialize()` auto-bind `$this` to the Plugin object — no explicit `use ($this)` needed
---

## 2026-03-25 — US-003
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Registered `template:task:show:top` → `Portfolio:widget/task_context_banner`: shows "This task is in Portfolio: {name}" banner with links to portfolio dashboards; nothing when no portfolios
- Registered `template:project:header:after` → `Portfolio:widget/project_header_badge`: shows portfolio membership badge(s) with links; nothing when no portfolios  
- Registered `template:board:task:icons` → `Portfolio:widget/board_task_blocked_icon`: shows 🔴 icon using PortfolioHelper::isTaskBlocked cache (same callable as board:task:footer — no extra queries)
- All three callables use defensive `$params['task|project'] ?? $params` param extraction
- Added `'This task is in Portfolio:'` to `Locale/en_US/translations.php`
- Added 6 template rendering tests to `WidgetHookTest.php` (2 per hook: render + nothing)
- Updated `testRegistersAllRequiredTemplateHooks` + `testTemplateHooksPointToCorrectWidgetTemplates` in `ApiRegistrationTest.php`
- 182 total tests pass (was 176), PHPCS 0 errors, PHPStan clean
- Files changed: `Plugin.php`, `Template/widget/task_context_banner.php`, `Template/widget/project_header_badge.php`, `Template/widget/board_task_blocked_icon.php`, `Locale/en_US/translations.php`, `Test/Controller/WidgetHookTest.php`, `Test/Plugin/ApiRegistrationTest.php`
- **Learnings for future iterations:**
  - `template:task:show:top` params structure: `$params['task'] ?? $params` → read `project_id` from task array
  - `template:project:header:after` params structure: `$params['project'] ?? $params` → read `id` from project array
  - Board task icons hook is identical in structure to board task footer hook — reuse the same callable shape
  - Use `&#x1F534;` HTML entity for 🔴 emoji in PHP template files to avoid file encoding issues
  - Codebase patterns section in progress.txt: add reusable learnings about params structures and test update procedures
---

## 2026-03-25 — US-004
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Registered `task.move.column` dispatcher listener in Plugin.php
  - Guards: task_id absent → return; config disabled → return; column not done-pattern → return; no milestones → return
  - Done-pattern list matches `PortfolioViewController::resolveCanonicalLane`: ['done','completed','closed','finished','deployed','released']
  - Column title: reads `$event['column_title']` first; falls back to DB lookup via `column_id` if empty
  - For each non-completed milestone containing the task: checks all tasks for is_active=0; calls milestoneModel->update($id, ['status'=>0]) if all done
- Added `portfolio_auto_complete_milestones` config (default 1) to ConfigController::getDefaultSettings(), normalizeSubmittedSettings()
- Added checkbox to Template/config/settings.php with portfolio-config-help text
- Added 2 new translation strings to Locale/en_US/translations.php
- Updated FakeMilestoneTaskModel (added getMilestones/getTasks + milestonesForTask/tasksForMilestone props)
- Added FakeMilestoneModel (updateCalls recording), FakeConfigModel (get() with defaults)
- Added 3 new tests to ApiRegistrationTest (185 total, was 182): auto-complete triggers, no-op on disabled config, no-op when tasks still active
- PHP lint clean, PHPCS 0 errors, PHPStan clean
- Files changed: Plugin.php, Controller/ConfigController.php, Template/config/settings.php, Locale/en_US/translations.php, Test/Plugin/ApiRegistrationTest.php
- **Learnings for future iterations:**
  - `task.move.column` event data: include `column_title` in test events; production Kanboard event likely includes it from task row joins
  - When adding listener tests with fake configModel: set `$this->plugin->container['configModel'] = $fake` AFTER `initialize()` (overrides lazy container entry)
  - Pre-commit phpstan timeout: cold Docker image needs ~40s; warm `ralphi check` first if needed (run it once before commit)
  - FakeMilestoneTaskModel is reused across US-002 (add tests) and US-004 (getMilestones/getTasks tests) — it's the shared stub for milestone-task relationship testing
---

## 2026-03-25 — US-005
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Registered `task.update` listener in Plugin.php → delegates to `DependencyModel::onTaskUpdated(int $taskId)` (stub, no-op, returns early)
- Registered `task.assignee_change` listener in Plugin.php → delegates to `PortfolioTaskModel::onAssigneeChanged(int $taskId)` (stub, no-op, returns early)
- Both listeners guard: `task_id <= 0` → return (defensive no-op)
- Added `onTaskUpdated()` stub to `Model/DependencyModel.php` with TODO comment (future: invalidate critical-path cache)
- Added `onAssigneeChanged()` stub to `Model/PortfolioTaskModel.php` with TODO comment (future: invalidate workload cache)
- Added `FakePortfolioTaskModel` to `Test/Plugin/ApiRegistrationTest.php` (assigneeChangedCalls tracking)
- Extended `FakeDependencyModel` with `$updatedCalls[]` + `onTaskUpdated()` method
- Added 4 new tests (189 total, was 185): testTaskUpdateListenerDelegatesToDependencyModelOnTaskUpdated, testTaskUpdateListenerIsNoopWhenTaskIdMissing, testTaskAssigneeChangeListenerDelegatesToPortfolioTaskModelOnAssigneeChanged, testTaskAssigneeChangeListenerIsNoopWhenTaskIdMissing
- PHP lint clean, PHPCS 0 errors, PHPStan clean, 189 tests pass
- Files changed: Plugin.php, Model/DependencyModel.php, Model/PortfolioTaskModel.php, Test/Plugin/ApiRegistrationTest.php
- **Learnings for future iterations:**
  - Stub listener pattern: register in Plugin.php → guard task_id → call model method → model method returns early with TODO doc comment
  - When adding new fake model classes to ApiRegistrationTest, follow the same stub shape: public array for call recording, method matching the real signature
  - For stub methods on existing models (DependencyModel, PortfolioTaskModel), insert before private helper methods so public API stays grouped at the top
---

## 2026-03-25 — US-006
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Added `->in('project_id', $projectIds)` to `buildFilteredTaskRows()` tasks query — eliminates full-table scan, scopes to portfolio projects before fetching
- Added `->in('task_id', $scopedTaskIds)` (where `$scopedTaskIds = array_keys($stats)`) to `buildTaskDependencyStats()` task_has_links query — scopes link lookup to portfolio task IDs only
- Empty-projectIds early return (already present before the `->in()` call) and empty-stats early return (already present before the link query) guard against empty IN lists
- `buildTableLookup()` for 'projects', 'columns', 'users' unchanged (small reference tables)
- Added `in()` method to `PortfolioTaskTestQueryBuilder` in test harness with proper `IN (...)` SQL generation; handles empty-values edge case with `1=0`
- Updated `buildWhereClause()` in test builder to dispatch on `IN` vs scalar operators
- All 189 existing tests pass unchanged — behavior identical, just fewer rows scanned
- PHP lint clean, PHPCS 0 errors, PHPStan clean
- Files changed: `Model/PortfolioTaskModel.php`, `Test/Model/PortfolioTaskModelTest.php`
- **Learnings for future iterations:**
  - `array_keys($stats)` gives portfolio-scoped task IDs for the `buildTaskDependencyStats()` IN clause — `$stats` is already filtered by `$projectScope`, so its keys are correct regardless of whether `$taskMap` came from a scoped or full-table query
  - Empty IN list guard pattern: early return before `->in()` call; test builder maps empty array to `1=0` as a safety net
  - PHPStan cold Docker timeout: warm with one manual `bash scripts/docker-phpstan.sh` before committing — the second run is fast
  - `PortfolioTaskTestQueryBuilder` IN implementation: multi-placeholder approach `:in_{conditionIndex}_{valueIndex}` avoids placeholder collisions when multiple IN conditions are chained
---

## 2026-03-25 — US-007
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- In `getDependencies()`, added a two-step query scoping:
  1. `tasks->in('project_id', $projectIds)->findAll()` fetches portfolio task IDs
  2. Early return if `$portfolioTaskIds === []` (no tasks in portfolio's projects)
  3. `task_has_links->in('task_id', $portfolioTaskIds)->findAll()` replaces the full-table scan
  4. Fallback to full-table scan when `count($portfolioTaskIds) > 1000` (SQLite SQLITE_MAX_VARIABLE_NUMBER limit)
- Added `in()` method to `DependencyTestQueryBuilder` in `Test/Model/DependencyModelTest.php`
- Updated `buildWhereClause()` in `DependencyTestQueryBuilder` to dispatch on `IN` vs scalar operators (same logic as PortfolioTaskTestQueryBuilder)
- All 189 existing tests pass unchanged — behavior identical, fewer rows scanned
- PHP lint clean, PHPCS 0 errors, PHPStan clean
- Files changed: `Model/DependencyModel.php`, `Test/Model/DependencyModelTest.php`
- **Learnings for future iterations:**
  - Both IN-scoping patterns (PortfolioTaskModel + DependencyModel) follow the same two-step fetch: portfolio task IDs first, then scoped link query with empty-guard + large-N fallback
  - `DependencyTestQueryBuilder` and `PortfolioTaskTestQueryBuilder` are now structurally identical — any future IN needs in either test file should follow the same `in()` + `buildWhereClause()` dispatch pattern
  - >1000 threshold is documented in comments but not tested explicitly (threshold testing would require >1000 fixture rows — impractical in unit tests; the comment is the contract)
  - PHPStan cold Docker timeout: always warm image before commit when switching machines or after long idle
---
